% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_report.R
\name{generate_data_report}
\alias{generate_data_report}
\title{Generate data coverage reports by institutional family}
\usage{
generate_data_report(
  data = readRDS(system.file("extdata", "compiled_indicators.rds", package = "cliaretl")),
  families = list_family_names(),
  country_id = country_code,
  year_id = year,
  ref_year = attr(metadata, "ref_year"),
  country_region_list = cliaretl::wb_income_and_region,
  metadata = cliaretl::db_variables,
  output_dir = tempdir(),
  output_type = c("docx", "html")
)
}
\arguments{
\item{data}{A country-year panel with one column per indicator. Defaults to
the compiled indicators panel shipped in `inst/extdata`.}

\item{families}{A character vector of institutional family names. Defaults
to all families in [family_order], in their dashboard order.}

\item{country_id}{A column identifying countries (e.g., `country_code`).}

\item{year_id}{A column identifying years.}

\item{ref_year}{Reference year passed to [compute_coverage()]. Defaults to
the `ref_year` attribute of `metadata`.}

\item{country_region_list}{A data frame with columns `country_code` and
`region`. Defaults to [wb_income_and_region].}

\item{metadata}{A variable metadata table. Defaults to [db_variables].}

\item{output_dir}{Directory in which to write the reports.}

\item{output_type}{Format of the reports: `"html"` (default) or `"docx"`.
Also passed to the report template as `params$output_type`.}
}
\value{
Invisibly, a named character vector with the path to each report.
}
\description{
Renders one HTML or Word report per institutional family. Each report has two
sections: (1) the family's variables, with their definitions and sources
(see [get_family_variables()]); and (2) the coverage table produced by
[compute_coverage()] for those variables (see [compute_family_coverage()]).
}
\examples{
\dontrun{
generate_data_report(families = "Political Institutions")
generate_data_report(families = "Political Institutions", output_type = "docx")
}

}
