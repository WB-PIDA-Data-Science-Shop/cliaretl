% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_quality_control.R
\name{interrogate_quality}
\alias{interrogate_quality}
\title{Interrogate data quality with pointblank}
\usage{
interrogate_quality(
  data,
  vars = dplyr::everything(),
  missing_threshold = 0.1,
  max_unique = 10
)
}
\arguments{
\item{data}{A data frame or tibble.}

\item{vars}{A tidyselect specification of columns to check. Defaults to all columns.}

\item{missing_threshold}{Numeric in \[0,1\], maximum allowed missing proportion (default = 0.1).}

\item{max_unique}{Integer, maximum allowed number of unique values for discrete checks (default = 10).}
}
\value{
A \pkg{pointblank} agent object that contains the results of the interrogation.
}
\description{
Runs a suite of custom data quality checks on selected columns using
\pkg{pointblank}. By default, checks are applied to all columns in the
input data. The checks include:
\itemize{
  \item \code{col_vals_no_outliers()}: Flags numeric values that are outliers (IQR rule).
  \item \code{col_vals_missing_below()}: Fails if missing rate exceeds a threshold.
  \item \code{col_vals_discrete()}: Fails if a variable has more unique values than allowed.
}
}
\examples{
library(pointblank)
df <- tibble::tibble(
  x = c(rnorm(95), 50, -40, NA),
  y = sample(letters, 100, replace = TRUE),
  z = sample(c("A", "B"), 100, replace = TRUE)
)

agent <- interrogate_quality(df, vars(x, z))
agent

}
